{
  "name": "api-web-testing-skills-poc",
  "version": "1.0.0",
  "private": true,
  "description": "Cursor Skills proof of concept for API and Web UI test generation.",
  "scripts": {
    "test:web": "playwright test",
    "test:web:headed": "playwright test --headed",
    "test:web:report": "playwright show-report",
    "install:web-browsers": "playwright install --with-deps chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "@types/node": "^25.9.1",
    "typescript": "^6.0.3"
  }
}
